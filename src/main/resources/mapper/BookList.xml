<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.web.dao.~~~~~~">


	<select id="list" resultType="com.example.web.dto.BookList">
		SELECT a.seq, a.code, a.title, a.writer, a.price, b.classification, a.minu
		FROM book_list a
		JOIN book_classification b ON a.code = b.code
	</select>

</mapper>






















